import { calcularCambio } from './cambio';
//the index.ts in carpeta cambio enables you to import everything from the cambio folder(dot know why)


const billetes = [50, 20, 10, 5, 2, 1, 0.5, 0.2, 0.1]

//function that calculates change

//function that takes the price of obj and the amount of money given
//the change is put vs each note/money size from the array and if it can be divided that goes to the solution and the rest is put against the next biggest note/coin

// const calculateChange = (price: number, amountPaid: number): number[] => {
//     if(price && amountPaid) {
//         billetes.map(billete => {
//             price/billete
//         })
//     }
// } 


